import { type Project } from '../projects';
import type { ArrayOf, Version } from './types';
export type Urls = ArrayOf<string, 3>;
export declare function getAvailableVersions(project: Project): Promise<string[]>;
export declare function getBaseUrls(project: Project, version: Version): Promise<Urls>;
export declare function getSnarkArtifactUrls(project: Project, options?: {
    parameters?: (bigint | number | string)[];
    version?: Version;
    cdnUrl?: string;
}): Promise<{
    wasms: [string, string, string];
    zkeys: [string, string, string];
}>;
